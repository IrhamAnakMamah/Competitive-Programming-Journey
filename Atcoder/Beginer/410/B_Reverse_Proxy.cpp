#include <bits/stdc++.h>
using namespace std;

int main(){
  int n,q;
  cin >> n >> q;

  vector<int> cek(n+1,0);
  int sisa = n;
  int bola = 0;
  for (int i = 0; i < n; i++)
  {
    int a;
    cin >> a;
    
  }
  
}