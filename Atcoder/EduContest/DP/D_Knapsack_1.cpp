#include <bits/stdc++.h>
using namespace std;

using ll = int64_t;

int main(){
  int t = 1;
  // cin >> t;
  while (t--)
  {
    int n;
    cin >> n;
    ll a[n][2];
    for (int i = 0; i < n; i++)
    {
      cin >> a[i][0] >> a[i][1];
    }
    
  }
  
}